# ----------------------------------------------------------------------------
# demo

if (USE_MATHS)
  include_directories ("${PROJECT_SOURCE_DIR}/maths")
  add_subdirectory (maths)
endif (USE_MATHS)

# ----------------------------------------------------------------------------
